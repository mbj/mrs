# Changelog

## 0.0.1

- Initial release

### Added

- `pg-ephemeral` subcommand for multi-platform gem building, testing, and publishing
- `release` subcommand for creating GitHub edge releases with artifacts
- `repository-lint` subcommand for verifying Rust version consistency
- `stratosphere` subcommand for syncing CloudFormation specification code generation
