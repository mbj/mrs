[workspace]
members = ["file-buf", "mlambda", "mmigration", "pg-client", "pg-ephemeral", "stack-deploy"]
resolver = "3"

[workspace.package]
edition = "2024"

[workspace.dependencies]
sqlx = { version = "0.8.6", features = ["postgres", "runtime-tokio"] }
