//! Diagnostics about this binary itself.
//!
//! Today: the integration test trial registry exposed via
//! `pg-ephemeral meta test` (see [`test`]). Future room for `meta info`,
//! `meta diagnose`, etc.

pub mod test;
